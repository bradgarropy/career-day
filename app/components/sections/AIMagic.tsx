import { Section } from "../Section";

export const AIMagic = () => {
  return (
    <Section id="ai-magic">
      <h1 className="text-[8rem] font-extrabold leading-none m-0 text-center">Magic 🪄</h1>

      <p className="text-5xl font-bold text-center mt-8 max-w-[60vw] leading-tight">
        Computers can <em>build things</em> when you just <strong>ASK</strong> them.
      </p>
    </Section>
  );
};
